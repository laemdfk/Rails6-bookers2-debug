
<%= form_with(model:[book, book_book_comment_path], local: true) do |f| %>
 <%= f.text_area :comment, rows:'5',placeholder: "コメント", class: "w-100" %>
  <%= f.submit "送信する", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
    <% end %>